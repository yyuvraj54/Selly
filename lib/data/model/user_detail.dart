import 'package:sellingportal/data/model/user_model.dart';

class UserDetail{
  final UserModel userModel;
  final List? bearerToken;
  UserDetail({required this.userModel,required this.bearerToken});

}