import 'package:flutter/material.dart';

class myAccountsPage extends StatelessWidget {
  const myAccountsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('My account Page'));
  }
}
